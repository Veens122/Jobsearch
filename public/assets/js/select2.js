$(document).ready(function () {
    $(".select2").select2({
        width: "100%",
        dropdownAutoWidth: true,
        minimumResultsForSearch: 10, // shows search box only if >10 items
    });
});
